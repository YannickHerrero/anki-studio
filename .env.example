# Port the Fastify server listens on
PORT=5174

# Override the ffmpeg binary location if it's not on PATH
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg

# Directory for per-session media and temp files
TMP_DIR=./tmp
